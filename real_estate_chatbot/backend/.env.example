PORT=4000
MONGO_URI=mongodb://localhost:27017/agent_mira_chatbot
CORS_ORIGIN=http://localhost:5173

# Optional — enables free-form NLP chat input via OpenAI.
# Without these, the chatbot falls back to the deterministic state machine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
